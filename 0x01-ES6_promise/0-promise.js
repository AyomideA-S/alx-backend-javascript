/* eslint-disable */
function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // For now, we don't need to do anything specific here
    // Just return a promise that doesn't resolve or reject
  });
}

export default getResponseFromAPI;
